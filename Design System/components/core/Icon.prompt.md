Line icon from Lucide (substituted set — the brand has no native icon library).

```jsx
<Icon name="calendar" size={24} />
```

Use sparingly: the brand leans on illustration, not iconography. Black strokes only; no filled or duotone icons.
