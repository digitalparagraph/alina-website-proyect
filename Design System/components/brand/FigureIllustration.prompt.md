Brand illustration. Use on a butter field (`--butter-400`) — the artwork's own background matches it.

```jsx
<FigureIllustration pose="point" height={420} base="../assets/illustrations/" />
```

Never redraw or recolour the figure; only these two packaged poses exist.
