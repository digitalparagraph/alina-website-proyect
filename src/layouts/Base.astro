---
import '@fontsource/poppins/latin-300.css';
import '@fontsource/poppins/latin-700.css';
import '../styles/tokens.css';
import '../styles/base.css';
import Persona from '../components/schema/Persona.astro';

const { titulo, descripcion, noindex = false } = Astro.props;

// Mientras no haya dominio, el despliegue es de staging y no debe indexarse.
// Un sitio de pruebas con el copy real indexado antes de tiempo compite con
// el definitivo y expone borradores. Se apaga con PUBLIC_STAGING=false.
const staging = import.meta.env.PUBLIC_STAGING !== 'false';
const sitio = Astro.site?.href.replace(/\/$/, '') ?? '';
const canonical = new URL(Astro.url.pathname, Astro.site).href;

const nav = [
  { href: '/proceso/', texto: 'El proceso' },
  { href: '/temas/', texto: 'Temas' },
  { href: '/terapia-o-coaching/', texto: 'Enfoques' },
  { href: '/sobre-alina/', texto: 'Sobre Alina' },
];
---
<html lang="es-MX">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>{titulo}</title>
  <meta name="description" content={descripcion} />
  <link rel="canonical" href={canonical} />
  {(noindex || staging) && <meta name="robots" content="noindex,nofollow" />}
  <meta property="og:title" content={titulo} />
  <meta property="og:description" content={descripcion} />
  <meta property="og:type" content="website" />
  <meta property="og:locale" content="es_MX" />
  <Persona sitio={sitio} />
  <slot name="schema" />
</head>
<body>
  {staging && (
    <p class="banda-staging">
      Sitio en revisión. Contenido pendiente de aprobación, no público.
    </p>
  )}

  <a href="#contenido" class="saltar">Saltar al contenido</a>

  <header class="cabecera">
    <div class="contenedor cabecera-fila">
      <a href="/" class="marca" aria-label="Alina Martínez, inicio">
        <img src="/logo/alina-logo.svg" alt="Alina Martínez · existir consciente"
             width="200" height="127" />
      </a>
      <nav aria-label="Principal">
        <ul>
          {nav.map((i) => <li><a href={i.href}>{i.texto}</a></li>)}
          <li><a href="/agendar/" class="boton">Agendar</a></li>
        </ul>
      </nav>
    </div>
  </header>

  <main id="contenido"><slot /></main>

  <footer class="pie">
    <div class="contenedor pila">
      <div class="pie-marca">
        <img src="/logo/alina-iso-morado.svg" alt="" width="56" height="56" aria-hidden="true" />
        <p class="eyebrow">existir consciente</p>
      </div>
      <p class="medida">
        Coaching terapéutico en línea. Sesiones individuales de aproximadamente
        50 minutos, habitualmente cada 15 días.
      </p>
      <ul class="pie-enlaces">
        <!-- /sobre-alina/ va en el pie de todas las páginas: E-E-A-T pide que
             la autoría sea alcanzable desde cualquier punto del sitio. -->
        <li><a href="/sobre-alina/">Sobre Alina</a></li>
        <li><a href="/alcance-del-servicio/">Alcance del servicio</a></li>
        <li><a href="/aviso-de-privacidad/">Aviso de privacidad</a></li>
        <li><a href="/terminos-y-condiciones/">Términos y condiciones</a></li>
      </ul>
    </div>
  </footer>
</body>
</html>

<style>
  /* max-width explícito: la regla de medida de lectura aplica a todo p y aquí
     recortaba la banda a 62 caracteres en lugar de cruzar la pantalla. */
  .banda-staging{margin:0;max-width:none;padding:var(--space-2) var(--space-5);
    text-align:center;background:var(--ink-800);color:var(--text-inverse);
    font-size:var(--text-xs);letter-spacing:var(--tracking-wide)}

  .saltar{position:absolute;left:-9999px;top:var(--space-3)}
  .saltar:focus{left:var(--space-5);background:var(--surface-card);padding:var(--space-3) var(--space-4);z-index:10}

  .cabecera{background:var(--surface-card);border-bottom:1px solid var(--border-subtle)}
  .cabecera-fila{display:flex;flex-wrap:wrap;gap:var(--space-4);
    align-items:center;justify-content:space-between;padding-block:var(--space-4)}
  .marca{text-decoration:none;display:block;line-height:0}
  .marca img{width:clamp(112px,16vw,175px);height:auto}
  nav ul{display:flex;flex-wrap:wrap;gap:var(--space-3) var(--space-5);
    align-items:center;list-style:none;margin:0;padding:0;font-size:var(--text-sm)}

  @media (max-width:640px){
    .cabecera-fila{padding-block:var(--space-3);gap:var(--space-3)}
    nav ul{gap:var(--space-2) var(--space-4);font-size:var(--text-xs);justify-content:flex-end}
    nav ul .boton{padding:var(--space-2) var(--space-4)}
  }

  .pie{border-top:1px solid var(--border-subtle);
    padding-block:var(--space-8);background:var(--butter-100)}
  .pie-marca{display:flex;align-items:center;gap:var(--space-3)}
  .pie-marca img{width:48px;height:auto}
  .pie-marca p{margin:0}
  .pie-enlaces{display:flex;flex-wrap:wrap;gap:var(--space-2) var(--space-5);
    list-style:none;margin:0;padding:0;font-size:var(--text-sm)}
</style>
