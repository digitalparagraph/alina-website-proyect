{
  "name": "alina-website",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verificar": "npm run build && npm run verificar:enlaces && npm run verificar:schema",
    "verificar:enlaces": "npx --yes linkinator dist --config linkinator.config.json",
    "verificar:schema": "node scripts/verificar-datos-estructurados.mjs"
  },
  "dependencies": {
    "@astrojs/sitemap": "^3.2.1",
    "@fontsource/poppins": "^5.1.0",
    "astro": "^5.1.1"
  }
}
